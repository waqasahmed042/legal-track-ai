/* eslint-disable prettier/prettier */
export interface preferredProps {
    selectedCardData: any;
}

export interface TabsProps {
    selectedCardData: any;
}

export interface prefferedProps {
    preferredStrikedClause: string;
    selectedCardData: any;
    isLoading: boolean;
}

export interface acceptableProps {
    acceptableStrikedClause: string;
    selectedCardData: any;
    isLoading: boolean;
}

export interface unacceptableProps {
    unacceptableStrikedClause: string;
    selectedCardData: any;
}